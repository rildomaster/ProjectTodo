import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        backgroundColor: '#0D0D0D',
        paddingTop: 30,
        paddingBottom: 50, //60
        alignItems: 'center'
    },
    logo: {

    }
});